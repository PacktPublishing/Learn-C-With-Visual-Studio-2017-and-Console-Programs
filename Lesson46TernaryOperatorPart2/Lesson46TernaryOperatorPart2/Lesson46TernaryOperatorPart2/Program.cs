using static System.Console;
class Program
{
    static void Main()
    {
        //bool result = ReadLine().Contains(ReadLine()) ? true : false;
        //WriteLine(result);
        WriteLine(ReadLine().Contains(ReadLine()));
        WriteLine(bool.TrueString);
        
    }
}